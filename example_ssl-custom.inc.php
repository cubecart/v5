<?php

// Example ssl-custom.inc.php file.
// Rename this if needed to detect that a store is running under SSL using a method other
// than what's in the stock ini.inc.php file.

// The if statement what you'll need to modify/use.  Be sure to leave the rest or things break.

//if($_SERVER["HTTP_X_FORWARDED_SSL"] == 1) {
//	define('CC_SSL', true);
//} else {
//	define('CC_SSL', false);
//}
